{
  "name": "front",
  "version": "0.0.1",
  "author": "Marcos Scheunemann",
  "private": true,
  "scripts": {
    "start:dev": "npx ionic serve",
    "start": "npx ng serve",
    "build": "npx ng build",
    "build:ionic": "npx ionic build --prod",
    "watch": "npx ng build --watch --configuration development",
    "test": "npx ng test",
    "lint": "npx ng lint"
  },
  "description": "An Ionic project"
}
